{
  "$schema": "https://json.schemastore.org/package.json",
  "type": "module",
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.8"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
