{
  "$schema": "https://json.schemastore.org/package.json",
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.8"
  },
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
